{
  "name": "weatherapp",
  "version": "0.1.0",
  "homepage": "https://bestmusman.github.io/React-Weather-App",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "master -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
